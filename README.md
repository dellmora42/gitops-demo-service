# Gitops Demo Service Project

## Overview

A service used to help learn the gitops and observability process.  Contains a small python service.

## Local Build

- `pip install build`
-  `pip install .`
- run `hello-cli`